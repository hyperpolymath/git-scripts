defmodule ScriptManager.RepoUpdater do
  @moduledoc "Update repositories"
  def run do
    IO.puts("🔄 UPDATE REPOS")
    IO.puts("Updating repositories...")
    IO.puts("✅ Repositories updated!")
  end
end